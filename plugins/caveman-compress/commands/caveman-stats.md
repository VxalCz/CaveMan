---
name: caveman-stats
description: Show token savings stats across all compressed files in a directory
allowed-tools: Bash
---

Show savings stats for the directory provided by the user (default: current directory).

Run:
```bash
cd "${CLAUDE_PLUGIN_ROOT}" && python -m scripts stats $ARGUMENTS
```

Report the total savings summary.
